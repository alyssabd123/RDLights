{
  "name": "RDLights",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "install-all": "npm install --workspaces"
  },
  "devDependencies": {
    "turbo": "^1.3.0"
  }
}
